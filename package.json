{
  "name": "javis-self-teaching-agent",
  "version": "2.0.0",
  "description": "Javis — a self-teaching browser agent: learn a task once via a DOM-reading agent, freeze the successful run into a stable-selector skill, then replay it zero-LLM with layered self-heal.",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "node build/esbuild.config.mjs",
    "dev": "node build/esbuild.config.mjs --watch"
  },
  "keywords": [
    "chrome-extension",
    "ai-agent",
    "browser-automation",
    "record-replay",
    "page-agent",
    "self-healing"
  ],
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.28.1",
    "jsdom": "^29.1.1",
    "vitest": "^4.1.8"
  }
}
